main() {
  // This is normal String
  String Name = "Samarpan KC";
  print(Name);

  // This is raw String
  String rawName = r"Samarpan/Kc";
  print(rawName);

  // Properties of string
  print(rawName.length); // Show the length of char
  print(rawName.padLeft(20)); // give padding
}
